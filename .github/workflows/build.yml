name: Build

on:
  workflow_call:
    inputs:
      tag:
        description: "Tag de version à publier (ex: V2.6.0 ou V2.6.0-rc.1)"
        required: true
        type: string
      release_name:
        description: "Titre de la release GitHub (ex: Stable 2.6.0 ou Snapshot 2.6.0-rc.1)"
        required: true
        type: string
      prerelease:
        description: "Marque la release comme pre-release (snapshot)"
        required: false
        type: boolean
        default: false

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Installer les dépendances
        run: pip install -r requirements.txt pyinstaller

      - name: Compiler l'application
        run: pyinstaller morphoz_snackapp.spec --clean --noconfirm

      - name: Archiver le build Windows
        shell: pwsh
        run: |
          Compress-Archive -Path dist/MorphozSnackApp -DestinationPath MorphozSnackApp-windows.zip

      - uses: actions/upload-artifact@v4
        with:
          name: build-windows
          path: MorphozSnackApp-windows.zip

  build-linux:
    name: Build Linux (${{ matrix.arch }})
    strategy:
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-latest
          - arch: aarch64
            runner: ubuntu-22.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Installer les dépendances système
        run: sudo apt-get install -y libusb-1.0-0-dev libgl1 libxcb-cursor0 libcairo2-dev

      - name: Installer les dépendances Python
        run: pip install -r requirements.txt pyinstaller

      - name: Compiler l'application
        run: pyinstaller morphoz_snackapp.spec --clean --noconfirm

      - name: Archiver le build Linux
        run: |
          tar -czf MorphozSnackApp-linux-${{ matrix.arch }}.tar.gz -C dist MorphozSnackApp

      - uses: actions/upload-artifact@v4
        with:
          name: build-linux-${{ matrix.arch }}
          path: MorphozSnackApp-linux-${{ matrix.arch }}.tar.gz

  release:
    name: Créer la release GitHub
    needs: [build-windows, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-windows

      - uses: actions/download-artifact@v4
        with:
          name: build-linux-x86_64

      - uses: actions/download-artifact@v4
        with:
          name: build-linux-aarch64

      - name: Publier la release avec les trois exécutables
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PRERELEASE_FLAG: ${{ inputs.prerelease && '--prerelease' || '' }}
        run: |
          gh release create "${{ inputs.tag }}" \
            MorphozSnackApp-windows.zip \
            MorphozSnackApp-linux-x86_64.tar.gz \
            MorphozSnackApp-linux-aarch64.tar.gz \
            --repo "${{ github.repository }}" \
            --title "${{ inputs.release_name }}" \
            --generate-notes \
            $PRERELEASE_FLAG
